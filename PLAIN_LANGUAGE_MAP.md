# PLAIN_LANGUAGE_MAP.md — 大白话文件地图

这份文档把项目里**每一个源代码文件**用最通俗的话讲清楚：它为什么存在、吃进什么吐出什么、靠谁、什么时候需要动它。
目标读者：**完全不懂编程的人**。

## 先看几个反复出现的词（只解释这一次）

- **网页应用**：用浏览器打开的那个网站版产品（在 `src/` 文件夹里）。
- **手机/电脑 App**：苹果手机和 Mac 上的那个独立程序，叫 QuickTrack（在 `ios/` 文件夹里）。
- **界面块**：屏幕上你能看到、能点的一块东西（按钮、卡片、弹窗、面板）。
- **逻辑模块**：一段不直接显示在屏幕上、专门负责"记住某类数据 + 处理某类事情"的可复用代码。
- **工具文件**：纯粹做计算或存放固定资料的小帮手，自己不显示任何界面。
- **云端账号**：登录后把数据存到网上的那套服务，换设备也能看到。
- **不确定**：凡是我没有十足把握的地方，都会直接写"不确定"，绝不瞎猜。

> 关于"依赖哪些文件"：网页应用部分是逐个核实过的（准确）。手机/电脑 App 部分，由于其代码天生互相共享，无法仅凭单个文件断定精确依赖，所以那部分按"大组关系"给出，精确到文件的标"不确定"。

---

# 第一部分：网页应用（`src/` 等）

## 启动相关

### `index.html`
- **目的**：浏览器打开网站时看到的第一个空壳页面，负责把整个网页应用拉起来。
- **吃进/吐出**：吃进——无；吐出——一个空白页面 + 启动指令（顺便加载数学公式显示库和字体）。
- **依赖**：启动 `src/main.jsx`。
- **何时需要改**：要换网页标题、网站图标、外部字体或公式库时。

### `src/main.jsx`
- **目的**：真正"开机"的总开关，把整个应用包上登录、语言、访问密码、出错保护这几层外壳。
- **吃进/吐出**：吃进——无；吐出——把应用挂到页面上开始运行。
- **依赖**：`App`、`PasswordGate`（访问密码）、`ErrorBoundary`（出错兜底）、`useAuth`（登录）、`useLanguage`（语言）。
- **何时需要改**：要调整"最外层包了哪些东西"或它们的先后顺序时。

### `src/App.jsx`
- **目的**：整个网页应用的"总指挥"，把所有数据和所有界面块串起来，决定此刻显示哪个画面、弹哪个窗。
- **吃进/吐出**：吃进——用户的各种操作；吐出——当前该显示的整套界面，以及完成步骤后的连锁反馈。
- **依赖**：几乎所有逻辑模块和界面块（60 多个）。
- **何时需要改**：新增一个大功能、加一个新弹窗、调整完成步骤后的庆祝/奖励流程时。

### `src/index.css`
- **目的**：全站的样式底子和动画效果定义。
- **吃进/吐出**：吃进——无；吐出——颜色、间距、动画等视觉规则。
- **依赖**：无（配合 Tailwind 样式工具）。
- **何时需要改**：要加全局动画或调整基础视觉时。

## 逻辑模块（`src/hooks/`）

### `src/hooks/useLocalStorage.js`
- **目的**：把一小块数据记在这台设备上，刷新或重开后还在。
- **吃进/吐出**：吃进——一个名字和初始值；吐出——当前值 + 一个"更新它"的开关。
- **依赖**：无（最底层，几乎所有其他模块都用它）。
- **何时需要改**：要改变"本机记忆"的基本读写方式时（影响面极大，慎改）。

### `src/hooks/useGameState.js`
- **目的**：管理任务、经验值、等级、连续天数；处理"完成/取消一个步骤"。
- **吃进/吐出**：吃进——勾选步骤等操作；吐出——最新的任务列表、经验值、等级、本次获得多少经验。
- **依赖**：`useLocalStorage`、`gameLogic`（计算）、`constants`（数值设定）。
- **何时需要改**：要改任务的结构、经验/升级/连签的算法时。

### `src/hooks/useRewardSystem.js`
- **目的**：管理金币钱包、随机惊喜金币、连签里程碑奖励、断签护盾。
- **吃进/吐出**：吃进——"完成了一步"等信号；吐出——钱包余额、是否中奖、流水记录。
- **依赖**：`useLocalStorage`、`constants`。
- **何时需要改**：要改奖励金额、概率、里程碑或护盾规则时。

### `src/hooks/useKnowledgeLore.js`
- **目的**：管理"知识碎片"的掉落与收集。
- **吃进/吐出**：吃进——完成步骤的信号；吐出——是否掉落了碎片、已收集清单。
- **依赖**：`useLocalStorage`、`loreData`（碎片内容）。
- **何时需要改**：要改掉落概率或碎片收集逻辑时。

### `src/hooks/useBlossomMode.js`
- **目的**：管理"概念逐步掌握"的进度（接触→锚定→深入等阶段）。
- **吃进/吐出**：吃进——接触/推进概念的操作；吐出——每个概念的掌握状态、当天推荐。
- **依赖**：`blossomData`（概念内容与规则）。
- **何时需要改**：要改掌握阶段、每日上限或推荐逻辑时。

### `src/hooks/useTheme.js`
- **目的**：记住并切换当前的配色主题。
- **吃进/吐出**：吃进——切换主题的操作；吐出——当前主题及其颜色。
- **依赖**：`useLocalStorage`、`constants`（主题清单）。
- **何时需要改**：要加新主题或改主题切换方式时。

### `src/hooks/useLanguage.jsx`
- **目的**：记住当前语言，并向全应用提供"按 key 取对应中/英文字"的能力。
- **吃进/吐出**：吃进——一个文字编号；吐出——对应语言的文字。
- **依赖**：`useLocalStorage`、`translations`（文案库）。
- **何时需要改**：要加语言或改取词方式时。

### `src/hooks/useAuth.jsx`
- **目的**：管理登录/注册/退出，告诉全应用"现在是谁、是否登录"。
- **吃进/吐出**：吃进——登录信息；吐出——当前用户、登录状态、各种登录动作。
- **依赖**：`supabase`（云端账号服务）。
- **何时需要改**：要加登录方式或改登录流程时。

### `src/hooks/useCloudSync.js`
- **目的**：登录后，让这台设备的数据和云端账号里的数据保持一致（双向）。
- **吃进/吐出**：吃进——本机数据变化；吐出——把变化上传、并在登录时把云端数据拉下来。
- **依赖**：`supabase`、`useAuth`、`migrateToCloud`。
- **何时需要改**：新增一类需要云端同步的数据、或排查同步问题时。

### `src/hooks/useCloudStorage.js`
- **目的**：另一种"按字段存云端"的存储帮手。
- **吃进/吐出**：吃进——表名/字段/初始值；吐出——该字段的云端值与更新开关。
- **依赖**：`supabase`、`useAuth`。
- **何时需要改**：**当前没有任何地方在用它**（属可清理的闲置文件）；若要改先确认是否还需要保留。

### `src/hooks/useAI.js`
- **目的**：保存各家智能服务的密钥与所选模型，并把智能功能提供给应用。
- **吃进/吐出**：吃进——用户填的密钥/选择；吐出——可用的智能调用与当前配置。
- **依赖**：`useLocalStorage`、`aiProviders`、`aiService`。
- **何时需要改**：要加智能服务商或改密钥管理时。

### `src/hooks/useDeadlineReminder.js`
- **目的**：在任务临近截止时发出提醒（需用户授权通知）。
- **吃进/吐出**：吃进——任务列表；吐出——到点的提醒（同一情形当天不重复）。
- **依赖**：`useLocalStorage`。
- **何时需要改**：要改提醒时机或去重规则时。

### `src/hooks/useTimer.js`
- **目的**：一个通用的倒计时/正计时器，给专注模式用。
- **吃进/吐出**：吃进——时长设定、开始/暂停；吐出——剩余/已用时间。
- **依赖**：无。
- **何时需要改**：要改计时行为时。

### `src/hooks/useSmartLauncher.js`
- **目的**：从所有步骤里挑出"当下最该做的一件"，并把卡太久的任务拆成小步。
- **吃进/吐出**：吃进——任务列表、精力情况；吐出——单个推荐步骤、停滞提示。
- **依赖**：`useLocalStorage`。
- **何时需要改**：要改"挑哪一步"的打分或拆分规则时。

### `src/hooks/useFrictionCalibrator.js`
- **目的**：记录每个步骤实际花了多久，和预期对比，找出"过难/过易"的步骤。
- **吃进/吐出**：吃进——步骤开始与完成的时刻；吐出——耗时与不匹配标记。
- **依赖**：`useLocalStorage`。
- **何时需要改**：要改预期时长或判定规则时。

### `src/hooks/useEnergyProfile.js`
- **目的**：记录你在一周各时段（早/中/晚）的精力高低。
- **吃进/吐出**：吃进——精力标记；吐出——精力档案与据此的难度建议。
- **依赖**：`useLocalStorage`。
- **何时需要改**：要改精力记录或建议方式时。

### `src/hooks/useGhostRace.js`
- **目的**：把"今天的完成进度"和"上周同一天"对比（自己跟自己赛跑）。
- **吃进/吐出**：吃进——完成记录；吐出——领先/落后状态。
- **依赖**：`useLocalStorage`。
- **何时需要改**：要改对比窗口或显示口径时。

### `src/hooks/useAccountabilityPact.js`
- **目的**：管理"押注一笔钱、在期限内完成若干步骤"的承诺契约。
- **吃进/吐出**：吃进——下注与完成进度；吐出——成功退款+奖励 / 失败没收。
- **依赖**：`useLocalStorage`、`constants`。
- **何时需要改**：要改下注上下限、奖励或判定时。

### `src/hooks/useParallelTracks.js`
- **目的**：让你在两个不同类别的任务之间来回切换（防无聊）。
- **吃进/吐出**：吃进——选定的两个任务；吐出——切换状态与进度。
- **依赖**：`useLocalStorage`。
- **何时需要改**：要改配对建议或切换逻辑时。

### `src/hooks/useStepCompletionChain.js`
- **目的**：完成一个步骤后，按顺序触发那一连串庆祝与奖励（经验、金币、碎片、推荐下一步等）。
- **吃进/吐出**：吃进——"哪个任务的哪一步完成了"；吐出——依次触发各种动画与奖励。
- **依赖**：`guidanceEngine`（下一步推荐）。
- **何时需要改**：要调整完成步骤后反馈的内容或先后顺序时。

### `src/hooks/useModalManager.js`
- **目的**：一个统一管理各种弹窗开关的帮手。
- **吃进/吐出**：吃进——开/关某弹窗的请求；吐出——各弹窗的开关状态。
- **依赖**：无。
- **何时需要改**：**当前没有任何地方在用它**（为"拆分总指挥"准备但还没接上）；动它前先确认。

### `src/hooks/useBudgetTracker.js`
- **目的**：个人记账/预算的逻辑（特意做得"无压力、不指责"）。
- **吃进/吐出**：吃进——收入/支出录入；吐出——各类预算的余量与状态颜色。
- **依赖**：`useLocalStorage`、`budgetDefaults`、`gameLogic`。
- **何时需要改**：要改预算分类或状态判定时。

### `src/hooks/useCopilot.js`
- **目的**：AI 聊天助手的对话逻辑（可附带文件）。
- **吃进/吐出**：吃进——你的提问、可选文件、当前游戏状态；吐出——AI 的回复。
- **依赖**：`aiService`、`fileExtractor`。
- **何时需要改**：要改助手的对话方式或它能看到的上下文时。

### `src/hooks/useVEMSync.js`
- **目的**：和一个外部"精力/状态地图"伴侣服务（项目里叫 **VEM**）对接：上报事件、取回当日"5 项指标"概览、提供小问卷反馈。
- **吃进/吐出**：吃进——应用内事件；吐出——当日精力概览、待回答的小反馈（失败的上报会暂存重试）。
- **依赖**：`useLocalStorage`、`useAuth`、`constants`。
- **何时需要改**：要改与该伴侣服务的对接方式时。**不确定**：该外部服务具体部署在哪里，本文档无法确认。

## 界面块（`src/components/`）

### `src/components/Header.jsx`
- **目的**：顶部栏，显示等级条、经验、连续天数、账号头像与同步状态。
- **吃进/吐出**：吃进——当前游戏状态、用户；吐出——顶部界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改顶部显示内容或入口按钮时。

### `src/components/ModeTabs.jsx`
- **目的**："学习/生活"两种模式的切换标签。
- **吃进/吐出**：吃进——当前模式；吐出——切换标签界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要加模式或改标签外观时。

### `src/components/QuestBoard.jsx`
- **目的**：主任务面板，按模式把任务、各种卡片板块组织在一起（二级总管）。
- **吃进/吐出**：吃进——任务与各板块数据；吐出——主面板界面。
- **依赖**：`QuestCard`、`PresetPicker`、`MicroLearn`、`RecentTasks`、`CollapsibleSection`、`SkillTree`、`ChallengeMode`、`DailyReflection`、`StudyRoadmap`、`LifeHabitDashboard`、`TodayDashboard`、`EnergyBudget`、`QuickAddTask`、`narrativeEngine`、`useLanguage`。
- **何时需要改**：要调整主面板里有哪些板块、怎么排时。

### `src/components/QuestCard.jsx`
- **目的**：单个任务的小卡片。
- **吃进/吐出**：吃进——一个任务；吐出——该任务的卡片界面。
- **依赖**：`constants`、`narrativeEngine`、`MathText`、`useLanguage`。
- **何时需要改**：要改任务卡的样子或卡上信息时。

### `src/components/QuestDetail.jsx`
- **目的**：单个任务的完整详情页，含步骤清单、进度、删除按钮（删除前会确认）。
- **吃进/吐出**：吃进——一个任务；吐出——详情界面与编辑/删除操作。
- **依赖**：`constants`、`useLanguage`、`timePredictor`、`narrativeEngine`、`ProgressRing`、`StepItem`、`MathText`。
- **何时需要改**：要改任务详情页布局或操作时。

### `src/components/StepItem.jsx`
- **目的**：单个步骤的一行（带勾选框、可拖动排序）。
- **吃进/吐出**：吃进——一个步骤；吐出——该步骤一行的界面。
- **依赖**：`constants`、`useLanguage`、`MathText`。
- **何时需要改**：要改步骤行外观或勾选/拖动行为时。

### `src/components/ProgressRing.jsx`
- **目的**：一个圆形进度环（很多地方复用）。
- **吃进/吐出**：吃进——一个百分比；吐出——画好的进度环。
- **依赖**：无。
- **何时需要改**：要改进度环样式时（牵动多处）。

### `src/components/MathText.jsx`
- **目的**：把含数学公式的文字正确显示出来（很多地方复用）。
- **吃进/吐出**：吃进——一段可能含公式的文字；吐出——渲染后的文字。
- **依赖**：无（配合外部公式库）。
- **何时需要改**：要改公式显示方式时（牵动多处）。

### `src/components/AnimatedBackground.jsx`
- **目的**：随主题变色的漂浮光球背景。
- **吃进/吐出**：吃进——当前主题；吐出——动态背景。
- **依赖**：无。
- **何时需要改**：要改背景动画时。

### `src/components/AuthModal.jsx`
- **目的**：登录/注册弹窗（邮箱、Google、GitHub）。
- **吃进/吐出**：吃进——用户输入；吐出——登录界面与结果提示。
- **依赖**：`useAuth`。
- **何时需要改**：要改登录界面或方式时。

### `src/components/PasswordGate.jsx`
- **目的**：共享部署时的访问密码门。
- **吃进/吐出**：吃进——访问密码；吐出——通过则放行，否则拦住。
- **依赖**：无。
- **何时需要改**：要改访问密码逻辑时。

### `src/components/AddQuestModal.jsx`
- **目的**：手动新建任务的弹窗。
- **吃进/吐出**：吃进——任务名等输入；吐出——一个新任务。
- **依赖**：`constants`、`useLanguage`、`gameLogic`。
- **何时需要改**：要改新建任务表单时。

### `src/components/QuickAddTask.jsx`
- **目的**：快速添加一个任务/步骤的简化入口。
- **吃进/吐出**：吃进——简短输入；吐出——新任务/步骤。
- **依赖**：`useLanguage`、`gameLogic`。
- **何时需要改**：要改快速添加方式时。

### `src/components/AIDecomposeModal.jsx`
- **目的**：用 AI 把一个大目标拆成步骤的弹窗（含深度选择与反复调整）。
- **吃进/吐出**：吃进——目标文字、深度选项；吐出——可预览确认的步骤列表。
- **依赖**：`constants`、`gameLogic`、`MathText`、`useLanguage`。
- **何时需要改**：要改拆解界面或调整选项时。

### `src/components/FileImportModal.jsx`
- **目的**：从上传文件导入/摘要成任务的弹窗。
- **吃进/吐出**：吃进——上传的文件；吐出——可用于建任务的结果。
- **依赖**：`useLanguage`、`fileExtractor`、`constants`、`MathText`。
- **何时需要改**：要改文件导入界面时。

### `src/components/BatchImportModal.jsx`
- **目的**：把粘贴/上传的大纲批量变成任务的弹窗（可加分组标签）。
- **吃进/吐出**：吃进——大纲文本；吐出——可勾选、可批量创建的任务。
- **依赖**：`useLanguage`、`constants`、`batchParser`、`fileExtractor`、`gameLogic`。
- **何时需要改**：要改批量导入界面时。

### `src/components/LifeHabitDashboard.jsx`
- **目的**：生活模式下的习惯仪表盘 + 每日时间块打卡（含每日自动重置、编辑）。
- **吃进/吐出**：吃进——习惯打卡数据；吐出——习惯进度与可勾选清单。
- **依赖**：`useLocalStorage`、`useLanguage`。
- **何时需要改**：要改习惯面板或打卡逻辑时。

### `src/components/BackpackPanel.jsx`
- **目的**：背包面板，汇总展示已掌握技能、已收集碎片、奖励里程碑。
- **吃进/吐出**：吃进——三类已有数据；吐出——汇总展示界面。
- **依赖**：`useLanguage`、`blossomData`、`constants`。
- **何时需要改**：要改背包展示内容时。

### `src/components/HyperfocusMode.jsx`
- **目的**：专注（沉浸）模式——无干扰计时执行步骤。
- **吃进/吐出**：吃进——选定时长与任务；吐出——专注界面与计时。
- **依赖**：`useLanguage`、`useTimer`、`ProgressRing`。
- **何时需要改**：要改专注模式界面或快捷键时。

### `src/components/BlossomPanel.jsx`
- **目的**：概念掌握的查看与操作面板。
- **吃进/吐出**：吃进——掌握进度；吐出——掌握面板界面。
- **依赖**：`useLanguage`、`blossomData`。
- **何时需要改**：要改掌握面板界面时。

### `src/components/LorePanel.jsx`
- **目的**：知识碎片/书籍的收藏面板（含掉落动画）。
- **吃进/吐出**：吃进——已收集碎片；吐出——收藏界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改碎片收藏界面时。

### `src/components/RewardPanel.jsx`
- **目的**：奖励面板——钱包、里程碑、护盾。
- **吃进/吐出**：吃进——钱包与里程碑状态；吐出——奖励界面。
- **依赖**：`useLanguage`、`constants`。
- **何时需要改**：要改奖励展示时。

### `src/components/SettingsPanel.jsx`
- **目的**：设置面板——智能服务密钥、主题、数据导入导出。
- **吃进/吐出**：吃进——设置项输入；吐出——已保存的设置与测试结果。
- **依赖**：`constants`、`aiProviders`、`useLanguage`。
- **何时需要改**：要加设置项或改设置界面时。

### `src/components/Timeline.jsx`
- **目的**：把任务按时间排成甘特图样式的时间线。
- **吃进/吐出**：吃进——带日期的任务；吐出——时间线界面。
- **依赖**：`useLanguage`、`constants`、`timePredictor`。
- **何时需要改**：要改时间线展示时。

### `src/components/SkillTree.jsx`
- **目的**：知识/技能树视图。
- **吃进/吐出**：吃进——进度数据；吐出——技能树界面。
- **依赖**：`constants`、`useLocalStorage`、`useLanguage`。
- **何时需要改**：要改技能树内容或外观时。

### `src/components/ChallengeMode.jsx`
- **目的**：每日挑战（小测验）系统。
- **吃进/吐出**：吃进——挑战与作答；吐出——挑战界面与统计。
- **依赖**：`constants`、`useLocalStorage`、`useLanguage`、`MathText`。
- **何时需要改**：要改挑战玩法时。

### `src/components/DailyReflection.jsx`
- **目的**：每日反思入口，统管 6 种反思方式 + 心情记录。
- **吃进/吐出**：吃进——选择的反思方式与输入；吐出——保存的反思记录。
- **依赖**：`useLocalStorage`、`useLanguage`、`reflectionModes`、以及 6 个反思子界面。
- **何时需要改**：要加反思方式或改反思入口时。

### `src/components/StudyRoadmap.jsx`
- **目的**：学习路线图视图（可看 AI 知识简介、记笔记）。
- **吃进/吐出**：吃进——路线进度与笔记；吐出——路线图界面。
- **依赖**：`useLocalStorage`、`useLanguage`、`aiService`、`MathText`。
- **何时需要改**：要改路线图内容或外观时。

### `src/components/SmartLauncher.jsx`
- **目的**："就做这一件"的防卡壳卡片界面。
- **吃进/吐出**：吃进——推荐步骤；吐出——单步卡片界面。
- **依赖**：`useLanguage`、`ProgressRing`。
- **何时需要改**：要改这张卡片的样子时。

### `src/components/EnergyPanel.jsx`
- **目的**：每周精力标记界面。
- **吃进/吐出**：吃进——精力标记；吐出——标记界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改精力标记界面时。

### `src/components/EnergyDashboard.jsx`
- **目的**：按精力安排任务的仪表盘。
- **吃进/吐出**：吃进——精力档案与任务；吐出——精力调度界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改精力调度展示时。

### `src/components/GhostRaceIndicator.jsx`
- **目的**：显示"对比上周自己"的领先/落后小徽章。
- **吃进/吐出**：吃进——对比结果；吐出——状态徽章。
- **依赖**：`useLanguage`。
- **何时需要改**：要改徽章显示时。

### `src/components/BossRush.jsx`
- **目的**：把逾期任务变成一场"Boss 战"来集中清掉。
- **吃进/吐出**：吃进——逾期任务；吐出——Boss 战界面与战斗进度。
- **依赖**：`useLanguage`、`ProgressRing`。
- **何时需要改**：要改 Boss 战玩法时。

### `src/components/AccountabilityPact.jsx`
- **目的**：押注承诺契约的界面。
- **吃进/吐出**：吃进——下注设定；吐出——契约界面与结果。
- **依赖**：`useLanguage`、`constants`、`ProgressRing`。
- **何时需要改**：要改契约界面时。

### `src/components/ParallelTracks.jsx`
- **目的**：双任务切换界面。
- **吃进/吐出**：吃进——两个任务；吐出——切换界面。
- **依赖**：`useLanguage`、`ProgressRing`。
- **何时需要改**：要改双轨切换界面时。

### `src/components/CalendarPanel.jsx`
- **目的**：日历视图 + 日程文件（日历文件）的导入导出。
- **吃进/吐出**：吃进——任务/日历文件；吐出——日历界面、可下载的日程文件。
- **依赖**：`useLocalStorage`、`useLanguage`、`icsService`、`gameLogic`。
- **何时需要改**：要改日历或导入导出时。

### `src/components/DailyPlanningModal.jsx`
- **目的**：用 AI 生成今日安排的弹窗（生活模式）。
- **吃进/吐出**：吃进——今日情况；吐出——一份每日计划。
- **依赖**：`useLanguage`、`aiService`、`gameLogic`。
- **何时需要改**：要改每日计划生成界面时。

### `src/components/FlyingXP.jsx`
- **目的**：完成步骤时，经验值从步骤位置"飞"到顶部计数器的动画。
- **吃进/吐出**：吃进——起点/终点与数值；吐出——飞行动画。
- **依赖**：无。
- **何时需要改**：要改这个动画时。

### `src/components/Celebrations.jsx`
- **目的**：经验弹跳、升级、任务完成的庆祝动画集合。
- **吃进/吐出**：吃进——庆祝触发；吐出——对应庆祝动画。
- **依赖**：`constants`、`MathText`、`useLanguage`。
- **何时需要改**：要改庆祝动画时。

### `src/components/OnboardingGuide.jsx`
- **目的**：第一次使用时的新手引导。
- **吃进/吐出**：吃进——是否首次；吐出——引导界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改新手引导内容时。

### `src/components/MicroLearn.jsx`
- **目的**：碎片化"微学习"小卡片。
- **吃进/吐出**：吃进——微学习内容；吐出——学习卡界面。
- **依赖**：`constants`、`useLocalStorage`、`useLanguage`、`MathText`。
- **何时需要改**：要改微学习玩法时。

### `src/components/RecentTasks.jsx`
- **目的**：最近完成的成就动态/链条。
- **吃进/吐出**：吃进——完成记录；吐出——动态列表界面。
- **依赖**：`constants`、`MathText`、`useLanguage`。
- **何时需要改**：要改成就动态展示时。

### `src/components/PresetPicker.jsx`
- **目的**：快速开始用的任务模板选择器。
- **吃进/吐出**：吃进——选择；吐出——按模板创建的任务。
- **依赖**：`constants`、`useLanguage`。
- **何时需要改**：要加模板或改选择器时。

### `src/components/CollapsibleSection.jsx`
- **目的**：可折叠的小节容器（手风琴）。
- **吃进/吐出**：吃进——标题与内容；吐出——可展开/收起的区块。
- **依赖**：`useLocalStorage`（记住展开状态）。
- **何时需要改**：要改折叠行为时。

### `src/components/StepCompleteGuide.jsx`
- **目的**：完成步骤后弹出的"接下来做什么"推荐卡（约 8 秒自动消失）。
- **吃进/吐出**：吃进——推荐内容；吐出——推荐卡界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改推荐卡外观或停留时间时。

### `src/components/ErrorBoundary.jsx`
- **目的**：出错保护网——某块界面崩了就显示兜底界面，而不是整页白屏。
- **吃进/吐出**：吃进——被它包住的界面；吐出——正常时透传、出错时显示兜底。
- **依赖**：无。
- **何时需要改**：要改出错兜底界面时。

### `src/components/TodayDashboard.jsx`
- **目的**："现在该做什么"的今日指挥中心，把若干今日信息聚成一处。
- **吃进/吐出**：吃进——今日任务与精力等；吐出——今日总览界面。
- **依赖**：`constants`、`useLanguage`、`MathText`、`ProgressRing`、`VEMWeatherCard`、`DailyProgressBar`。
- **何时需要改**：要改今日总览内容时。

### `src/components/DailyProgressBar.jsx`
- **目的**：今日完成进度条。
- **吃进/吐出**：吃进——今日进度；吐出——进度条界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改今日进度条时。

### `src/components/VEMWeatherCard.jsx`
- **目的**：用"天气"比喻显示精力预报的小卡片（5 个迷你指标环）。
- **吃进/吐出**：吃进——当日精力指标；吐出——精力天气卡。
- **依赖**：`useLanguage`、`constants`。
- **何时需要改**：要改精力天气卡时。

### `src/components/VEMQuickPanel.jsx`
- **目的**：精力地图的完整浮层（5 个大指标环、预报、精力预算概览）。
- **吃进/吐出**：吃进——精力数据；吐出——精力总览浮层。
- **依赖**：`useLanguage`、`constants`。
- **何时需要改**：要改精力总览界面时。

### `src/components/EnergyBudget.jsx`
- **目的**：按时间段显示"今日推荐做多少步"的精力预算（开启 VEM 时替代精力卡）。
- **吃进/吐出**：吃进——精力预算与已完成；吐出——预算界面。
- **依赖**：`useLanguage`。
- **何时需要改**：要改精力预算展示时。

### `src/components/MicroFeedbackChip.jsx`
- **目的**：完成步骤后偶尔弹出的小反馈问句（3 选项，约 6 秒消失），用于校准精力模型。
- **吃进/吐出**：吃进——一个小问题；吐出——你的快速选择。
- **依赖**：`useLanguage`。
- **何时需要改**：要改小反馈样式或时机时。

### `src/components/AICopilotPanel.jsx`
- **目的**：AI 聊天助手界面（可附文件、可选心情）。
- **吃进/吐出**：吃进——你的提问与文件；吐出——对话界面与回复。
- **依赖**：`useLanguage`、`fileExtractor`。
- **何时需要改**：要改助手聊天界面时。

### `src/components/BudgetDashboard.jsx`
- **目的**：个人记账仪表盘（含趋势折线图；颜色"只报信不指责"）。
- **吃进/吐出**：吃进——预算数据；吐出——记账图表界面。
- **依赖**：`useLanguage`、外部图表库 recharts。
- **何时需要改**：要改记账界面或图表时。

### 反思子界面（`src/components/reflections/`）

### `src/components/reflections/OneTapQuickMode.jsx`
- **目的**：一键快记心情 + "今天的小确幸"。
- **吃进/吐出**：吃进——一次轻点；吐出——一条快速记录。
- **依赖**：`reflectionModes`。
- **何时需要改**：要改一键快记时。

### `src/components/reflections/CampfireCheckIn.jsx`
- **目的**："篝火"式引导问答反思。
- **吃进/吐出**：吃进——逐步作答；吐出——一次篝火反思记录。
- **依赖**：`reflectionModes`。
- **何时需要改**：要改篝火问答时。

### `src/components/reflections/ChatCompanion.jsx`
- **目的**：和 AI 守护者聊天式反思。
- **吃进/吐出**：吃进——对话；吐出——陪伴式回应。
- **依赖**：`reflectionModes`。
- **何时需要改**：要改陪伴对话时。

### `src/components/reflections/PromptRoulette.jsx`
- **目的**：随机抽取一个反思提问（转盘）。
- **吃进/吐出**：吃进——转一下；吐出——一个随机提问与作答。
- **依赖**：`reflectionModes`。
- **何时需要改**：要改提问池或转盘时。

### `src/components/reflections/BodyTap.jsx`
- **目的**：在身体部位上标记当下感受。
- **吃进/吐出**：吃进——点选身体部位；吐出——身体感受记录。
- **依赖**：`reflectionModes`。
- **何时需要改**：要改身体感受映射时。

### `src/components/reflections/MoodTerrain.jsx`
- **目的**：把心情历史画成"地形图"。
- **吃进/吐出**：吃进——历史心情；吐出——地形可视化。
- **依赖**：`terrainGenerator`。
- **何时需要改**：要改地形可视化时。

## 工具文件（`src/utils/`）

### `src/utils/constants.js`
- **目的**：全应用的"设定大全"——分类、等级、经验数值、奖励、主题、预设排程等固定资料。
- **吃进/吐出**：吃进——无；吐出——各种固定设定供别处取用。
- **依赖**：`aiProviders`。
- **何时需要改**：要调任何核心数值或设定时（牵动极广，慎改）。

### `src/utils/gameLogic.js`
- **目的**：核心计算——根据经验算等级、算每步经验、算连续天数、生成唯一编号。
- **吃进/吐出**：吃进——经验/步骤/日期等；吐出——等级、经验值、连签、编号。
- **依赖**：`constants`。
- **何时需要改**：要改等级/经验/连签算法时。

### `src/utils/blossomData.js`
- **目的**：概念掌握的内容库与规则（约 80 个概念节点）。
- **吃进/吐出**：吃进——无；吐出——概念资料与阶段规则。
- **依赖**：无。
- **何时需要改**：要增改概念内容或掌握规则时。

### `src/utils/loreData.js`
- **目的**：可收集"知识碎片/书籍"的内容库（约 8 本、约 48 片）。
- **吃进/吐出**：吃进——无；吐出——碎片与书籍资料。
- **依赖**：无。
- **何时需要改**：要增改碎片内容时。

### `src/utils/guidanceEngine.js`
- **目的**：给"接下来做什么"的若干候选打分排序。
- **吃进/吐出**：吃进——刚完成的步骤、任务们；吐出——排好序的推荐。
- **依赖**：无。
- **何时需要改**：要改推荐排序规则时。

### `src/utils/timePredictor.js`
- **目的**：根据近期速度估算任务还要多久、是否赶得上截止。
- **吃进/吐出**：吃进——任务完成历史；吐出——速度与完成预估。
- **依赖**：无。
- **何时需要改**：要改预估算法时。

### `src/utils/translations.js`
- **目的**：所有中英文文案。
- **吃进/吐出**：吃进——无；吐出——按编号取用的中/英文字。
- **依赖**：无。
- **何时需要改**：要加/改任何界面文字时。

### `src/utils/aiProviders.js`
- **目的**：四家智能服务（Claude/GLM/DeepSeek/Qwen）的配置 + 统一的"问 AI"调用与连接测试。
- **吃进/吐出**：吃进——服务商、密钥、问题；吐出——AI 的回答。
- **依赖**：无。
- **何时需要改**：要加智能服务商或改统一调用时。

### `src/utils/aiService.js`
- **目的**：具体的 AI 功能（拆解目标、精修、出小测、摘要文件、生成每日计划、助手聊天）。
- **吃进/吐出**：吃进——目标/文件/对话等；吐出——结构化的 AI 结果。
- **依赖**：`aiProviders`。
- **何时需要改**：要加/改某个 AI 功能时。

### `src/utils/claudeClient.js`
- **目的**：较早的、只针对 Claude 的调用工具。
- **吃进/吐出**：吃进——请求；吐出——Claude 回答。
- **依赖**：无。
- **何时需要改**：**当前没有任何地方在用它**（已被 `aiProviders`/`aiService` 取代）；动它前先确认是否删除。

### `src/utils/batchParser.js`
- **目的**：把粘贴的大纲文字解析成"任务 + 步骤"的层次结构。
- **吃进/吐出**：吃进——一段大纲文本；吐出——分好层次的条目/任务。
- **依赖**：无。
- **何时需要改**：要改大纲识别规则时。

### `src/utils/fileExtractor.js`
- **目的**：把上传的文件读成纯文字，供导入或喂给 AI。
- **吃进/吐出**：吃进——文件；吐出——提取出的文字。
- **依赖**：无。
- **何时需要改**：要支持新文件类型时。

### `src/utils/narrativeEngine.js`
- **目的**：给每个任务生成一段 RPG 故事设定 + 季节性世界主题。
- **吃进/吐出**：吃进——一个任务；吐出——它的故事文案。
- **依赖**：无。
- **何时需要改**：要改故事生成或季节主题时。

### `src/utils/reflectionModes.js`
- **目的**：6 种反思方式的配置、提示语、身体部位等。
- **吃进/吐出**：吃进——无；吐出——反思相关配置。
- **依赖**：无。
- **何时需要改**：要改反思配置/提示语时。

### `src/utils/terrainGenerator.js`
- **目的**：根据心情数据算出"地形图"的绘制路径。
- **吃进/吐出**：吃进——一串数值；吐出——绘图路径。
- **依赖**：无。
- **何时需要改**：要改地形画法时。

### `src/utils/icsService.js`
- **目的**：生成和读取标准日历文件（把任务/习惯变成日历事件，或反过来）。
- **吃进/吐出**：吃进——任务/习惯 或 日历文件文本；吐出——日历文件文本 或 解析出的事件。
- **依赖**：无（下载时用到浏览器能力）。
- **何时需要改**：要改导出格式或解析时。

### `src/utils/budgetDefaults.js`
- **目的**：记账功能的占位默认数值（真实数值按用户存）。
- **吃进/吐出**：吃进——无；吐出——一套默认预算分类与金额。
- **依赖**：无。
- **何时需要改**：要改默认预算分类时。

## 云端连接（`src/lib/`）

### `src/lib/supabase.js`
- **目的**：与"云端账号/数据库"服务建立连接的唯一入口。
- **吃进/吐出**：吃进——配置；吐出——一个可用的云端连接（没配置则进入"仅本机"模式）。
- **依赖**：无。
- **何时需要改**：要换云端服务或改连接配置时。

### `src/lib/migrateToCloud.js`
- **目的**：用户**第一次登录**时，把这台设备上已有的数据一次性搬到云端。
- **吃进/吐出**：吃进——本机已有数据；吐出——把它们写到云端账号（只做一次）。
- **依赖**：`supabase`。
- **何时需要改**：要改首次登录的数据搬迁规则时。

---

# 第二部分：手机/电脑 App（`ios/` 里的 QuickTrack）

> 说明：这是苹果手机/Mac 上的独立程序，是网页版的"伴侣"。它读写**同一个云端账号**。
> 由于这类程序内部文件天生共享代码，**精确到文件的依赖无法只看单个文件断定，下面统一按大组关系给出，存疑处标"不确定"**。
> 大组关系（来自依赖分析）：界面 → 引擎/数据/模型/共享；引擎 → 数据/模型/共享；数据 → 模型/共享；共享 = 最底层公共件。

## 启动

### `ios/QuickTrack/QuickTrack/QuickTrackApp.swift`
- **目的**：App 的开机入口，启动时初始化共享存储。
- **吃进/吐出**：吃进——无；吐出——把主界面拉起。
- **依赖**：`ContentView` + 共享件（不确定到具体文件）。
- **何时需要改**：要改 App 启动行为时。

### `ios/QuickTrack/QuickTrack/ContentView.swift`
- **目的**：根界面，底部四个标签（今日 / 任务 / 成就 / 设置）。
- **吃进/吐出**：吃进——无；吐出——带标签栏的主界面。
- **依赖**：`TodayView`、`QuestsView`、`AchievementsView`、`SettingsView`。
- **何时需要改**：要加/改顶层标签时。

## 数据模型（`Models/`）

### `Models/Tracker.swift`
- **目的**：定义"一个追踪项"是什么。
- **吃进/吐出**：吃进——无；吐出——数据形状的定义。
- **依赖**：共享件（不确定到具体文件）。
- **何时需要改**：要改追踪项的字段时。

### `Models/TrackerSummary.swift`
- **目的**：统一的"展示用"数据形状（含动作项、步骤信息），界面到处都用它。
- **吃进/吐出**：吃进——无；吐出——展示用数据的定义。
- **依赖**：被界面与数据层广泛使用。
- **何时需要改**：要改界面展示所需的数据形状时。

## 数据来源与同步（`DataSources/`）

### `DataSources/SyncManager.swift`
- **目的**：同步总管——先本地立即更新，再尝试写云端；定时拉取最新。
- **吃进/吐出**：吃进——用户操作；吐出——本地即时变化 + 云端读写。
- **依赖**：`SupabaseClient`、`SupabaseAdapter`、`RetryQueue`、模型、共享件。
- **何时需要改**：要改同步/拉取/乐观更新策略时。

### `DataSources/RetryQueue.swift`
- **目的**：写云端失败时，按"越等越久"的方式自动重试。
- **吃进/吐出**：吃进——失败的写操作；吐出——稍后重试。
- **依赖**：被 `SyncManager` 使用。
- **何时需要改**：要改重试节奏时。

### `DataSources/SupabaseClient.swift`
- **目的**：与云端服务通话的底层客户端。
- **吃进/吐出**：吃进——请求；吐出——云端响应。
- **依赖**：共享件（配置/密钥）。
- **何时需要改**：要改与云端通话的底层方式时。

### `DataSources/SupabaseAdapter.swift`
- **目的**：把云端返回的数据翻译成 App 用的展示形状。
- **吃进/吐出**：吃进——云端数据；吐出——展示用数据。
- **依赖**：`SupabaseClient`、模型。
- **何时需要改**：云端数据结构变化时。

### `DataSources/HealthKitAdapter.swift`
- **目的**：把苹果健康数据翻译成 App 用的展示形状。
- **吃进/吐出**：吃进——健康数据；吐出——展示用数据。
- **依赖**：模型、苹果健康框架。
- **何时需要改**：要改读取的健康指标时。

### `DataSources/HealthKitAutoComplete.swift`
- **目的**：根据健康数据自动帮你"打勾"某些习惯。
- **吃进/吐出**：吃进——健康数据；吐出——自动完成的习惯。
- **依赖**：健康相关件。
- **何时需要改**：要改自动完成规则时。

### `DataSources/TrackerDataSource.swift`
- **目的**：一个统一的"数据来源"接口约定，让上层不关心数据具体来自哪。
- **吃进/吐出**：吃进——无；吐出——接口约定。
- **依赖**：模型。
- **何时需要改**：要改数据来源的统一约定时。

### `DataSources/LiveActivityManager.swift`
- **目的**：管理锁屏/灵动岛上的"任务进行中"实时活动。
- **吃进/吐出**：吃进——任务进行状态；吐出——锁屏实时显示。
- **依赖**：`QuestSessionAttributes`（共享）。
- **何时需要改**：要改实时活动行为时。

### `DataSources/WidgetRefreshPolicy.swift`
- **目的**：决定小组件多久刷新一次（省电）。
- **吃进/吐出**：吃进——情境；吐出——刷新时机。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改小组件刷新频率时。

### `DataSources/SampleData.swift`
- **目的**：开发预览用的示例假数据。
- **吃进/吐出**：吃进——无；吐出——一批演示数据。
- **依赖**：模型。
- **何时需要改**：要改预览示例时（不影响真实使用）。

## 玩法与反馈引擎（`Engine/`）

### `Engine/RewardChain.swift`
- **目的**：完成步骤后的奖励链（经验→金币→碎片→升级→任务完成），和网页版一致。
- **吃进/吐出**：吃进——一次完成；吐出——一串奖励结果。
- **依赖**：数据层、模型、共享件。
- **何时需要改**：要改奖励链内容/顺序时。

### `Engine/CelebrationQueue.swift`
- **目的**：让多个庆祝动画**排队**一个一个放，不打架。
- **吃进/吐出**：吃进——若干庆祝；吐出——按序播放。
- **依赖**：被界面使用。
- **何时需要改**：要改庆祝排队规则时。

### `Engine/CelebrationOverlays.swift`
- **目的**：各种庆祝浮层的样子（经验弹出、升级、任务完成、碎片掉落等）。
- **吃进/吐出**：吃进——庆祝类型；吐出——对应浮层界面。
- **依赖**：共享件。
- **何时需要改**：要改庆祝浮层外观时。

### `Engine/EngagementEngine.swift`
- **目的**：连击、个人最佳、得分等"上头"机制。
- **吃进/吐出**：吃进——完成事件；吐出——连击/纪录/分数。
- **依赖**：数据层、共享件。
- **何时需要改**：要改连击/纪录玩法时。

### `Engine/HapticEngine.swift`
- **目的**：按事件给出不同的振动反馈。
- **吃进/吐出**：吃进——事件类型；吐出——对应振动。
- **依赖**：被很多界面使用（很核心）。
- **何时需要改**：要改振动方式时（牵动多处）。

### `Engine/ThemeManager.swift`
- **目的**：6 套配色主题与全局上色。
- **吃进/吐出**：吃进——所选主题；吐出——全局颜色。
- **依赖**：共享件。
- **何时需要改**：要加/改主题时。

### `Engine/VisualEffects.swift`
- **目的**：彩带、粒子、涟漪等视觉特效，以及若干小特效视图。
- **吃进/吐出**：吃进——触发；吐出——特效。
- **依赖**：共享件。
- **何时需要改**：要改特效时。

### `Engine/GuidanceEngine.swift`
- **目的**：给"接下来做什么"打分排序（对应网页版同名工具）。
- **吃进/吐出**：吃进——当前步骤与任务；吐出——推荐。
- **依赖**：模型。
- **何时需要改**：要改推荐规则时。

### `Engine/WeeklyInsightEngine.swift`
- **目的**：生成每周回顾报告。
- **吃进/吐出**：吃进——一周数据；吐出——周报。
- **依赖**：数据层。
- **何时需要改**：要改周报内容时。

### `Engine/CompanionEngine.swift`
- **目的**：陪伴角色的逻辑（按心情/情境给出话语）。**按名称推断**。
- **吃进/吐出**：吃进——情境/心情；吐出——陪伴文案。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改陪伴角色行为时。

### `Engine/DailyContentEngine.swift`
- **目的**：每天轮换的内容（如每日一句/一题）。**按名称推断**。
- **吃进/吐出**：吃进——日期；吐出——当日内容。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改每日内容时。

### `Engine/TimeOfDayPalette.swift`
- **目的**：按一天中的时段给出不同配色/氛围。
- **吃进/吐出**：吃进——当前时间；吐出——对应色彩。
- **依赖**：共享件。
- **何时需要改**：要改时段配色时。

### `Engine/MysteryBoxStore.swift`
- **目的**："神秘盒子"奖励的逻辑与存取。**按名称推断**。
- **吃进/吐出**：吃进——开盒动作；吐出——盒中奖励。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改神秘盒玩法时。

### `Engine/DailyDiceStore.swift`
- **目的**："每日骰子"玩法的逻辑与存取。**按名称推断**。
- **吃进/吐出**：吃进——掷骰；吐出——结果。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改每日骰子玩法时。

### `Engine/MicroCommitmentStore.swift`
- **目的**："微承诺"（极小目标）的逻辑与存取。**按名称推断**。
- **吃进/吐出**：吃进——一个小承诺；吐出——其状态。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改微承诺玩法时。

### `Engine/MantraStore.swift`
- **目的**：每日格言/肯定句的存取。**按名称推断**。
- **吃进/吐出**：吃进——无；吐出——一句格言。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改格言内容/逻辑时。

### `Engine/DailyCheckInStore.swift`
- **目的**：每日签到/打卡流程的状态存取。**按名称推断**。
- **吃进/吐出**：吃进——签到动作；吐出——签到状态。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改签到逻辑时。

### `Engine/FocusFilterStore.swift`
- **目的**：专注模式的过滤/筛选设置存取。**按名称推断**。
- **吃进/吐出**：吃进——筛选设定；吐出——当前筛选。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改专注筛选时。

## 交互入口（`Intents/`，可从小组件/Siri 触发）

### `Intents/LogEventIntent.swift`
- **目的**：从小组件或 Siri 直接给某个习惯打勾。
- **吃进/吐出**：吃进——打勾指令；吐出——记录一次完成。
- **依赖**：数据层、共享件。
- **何时需要改**：要改小组件/语音打卡行为时。

### `Intents/CompleteStepIntent.swift`
- **目的**：从小组件直接完成一个步骤。
- **吃进/吐出**：吃进——完成指令；吐出——记录步骤完成。
- **依赖**：数据层、共享件。
- **何时需要改**：要改小组件完成步骤行为时。

### `Intents/RefreshSummaryIntent.swift`
- **目的**：强制刷新小组件显示。
- **吃进/吐出**：吃进——刷新指令；吐出——重新拉取并刷新。
- **依赖**：数据层。
- **何时需要改**：要改强制刷新行为时。

### `Intents/QuickTrackShortcuts.swift`
- **目的**：把上面这些操作登记成 Siri 快捷指令。
- **吃进/吐出**：吃进——无；吐出——可被系统/Siri 发现的快捷入口。
- **依赖**：上述各 Intent。
- **何时需要改**：要增改语音快捷指令时。

## 界面（`Views/`）

### `Views/TodayView.swift`
- **目的**：主仪表盘"今日"页（体量最大）。
- **吃进/吐出**：吃进——今日数据；吐出——今日主界面。
- **依赖**：`Today/` 下各子卡片、引擎、数据层。
- **何时需要改**：要改今日页内容时（建议优先把子块拆到 `Today/`）。

### `Views/QuestsView.swift`
- **目的**：任务列表页。
- **吃进/吐出**：吃进——任务们；吐出——任务列表界面。
- **依赖**：数据层、引擎。
- **何时需要改**：要改任务列表时。

### `Views/QuestDetailView.swift`
- **目的**：单个任务详情页。
- **吃进/吐出**：吃进——一个任务；吐出——详情界面。
- **依赖**：数据层、引擎。
- **何时需要改**：要改任务详情时。

### `Views/AddQuestView.swift`
- **目的**：手动新建任务页。
- **吃进/吐出**：吃进——输入；吐出——新任务。
- **依赖**：数据层。
- **何时需要改**：要改新建任务表单时。

### `Views/AchievementsView.swift`
- **目的**：成就/徽章/里程碑页。
- **吃进/吐出**：吃进——成就数据；吐出——成就界面。
- **依赖**：数据层、引擎。
- **何时需要改**：要改成就展示时。

### `Views/SettingsView.swift`
- **目的**：设置页（含登录、密钥配置等）。
- **吃进/吐出**：吃进——设置输入；吐出——已保存设置。
- **依赖**：数据层、共享件。
- **何时需要改**：要加/改设置项时。

### `Views/LoginView.swift`
- **目的**：登录页。
- **吃进/吐出**：吃进——登录信息；吐出——登录结果。
- **依赖**：数据层、共享件。
- **何时需要改**：要改登录界面时。

### `Views/TrendsView.swift`
- **目的**：趋势/图表页。
- **吃进/吐出**：吃进——历史数据；吐出——图表界面。
- **依赖**：数据层、图表框架。
- **何时需要改**：要改图表时。

### `Views/FocusTimerView.swift` 与 `Views/FocusModePicker.swift`
- **目的**：专注计时页 与 专注模式选择。
- **吃进/吐出**：吃进——时长/模式选择；吐出——专注计时界面。
- **依赖**：`FocusFilterStore` 等。
- **何时需要改**：要改专注计时玩法时。

### `Views/DailyCheckInFlow.swift` · `Views/QuickCheckInView.swift` · `Views/Today/CheckInSummaryCard.swift`
- **目的**：每日签到的完整流程 / 快速签到 / 签到小结卡。
- **吃进/吐出**：吃进——签到作答；吐出——签到记录与小结。
- **依赖**：`DailyCheckInStore` 等。
- **何时需要改**：要改签到流程时。
- **注意**：`QuickCheckInView.swift` **当前无任何地方引用**（疑似闲置/旧版），动它前先确认。

### `Views/QuickWinView.swift`
- **目的**："快速胜利"建议页。**按名称推断**。
- **吃进/吐出**：吃进——任务情况；吐出——一个易上手的建议。
- **依赖**：数据层。
- **何时需要改**：要改快速胜利建议时。

### `Views/MicroCommitmentCard.swift`
- **目的**："微承诺"卡片界面。
- **吃进/吐出**：吃进——一个小承诺；吐出——其卡片。
- **依赖**：`MicroCommitmentStore`。
- **何时需要改**：要改微承诺界面时。

### `Views/PostStepGuideCard.swift`
- **目的**：完成步骤后的"接下来做什么"卡。
- **吃进/吐出**：吃进——推荐；吐出——引导卡。
- **依赖**：`GuidanceEngine`。
- **何时需要改**：要改引导卡时。

### `Views/ConstellationMapView.swift`
- **目的**：把成就/技能画成"星座图"。**按名称推断**。
- **吃进/吐出**：吃进——进度数据；吐出——星图界面。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改星图时。

### `Views/TrophyRoomView.swift`
- **目的**：陈列已得奖杯的"奖杯室"。**按名称推断**。
- **吃进/吐出**：吃进——奖杯数据；吐出——奖杯室界面。
- **依赖**：不确定到具体文件。
- **何时需要改**：要改奖杯室时。

### `Views/SwipeActionRow.swift`
- **目的**：可左右滑动出操作的列表行（通用小件）。
- **吃进/吐出**：吃进——行内容与操作；吐出——可滑动行。
- **依赖**：无明显特定依赖。
- **何时需要改**：要改滑动操作行为时。

### `Views/TrackerListView.swift`
- **目的**：早期"追踪项列表"页。
- **吃进/吐出**：吃进——追踪项；吐出——列表界面。
- **依赖**：不确定到具体文件。
- **何时需要改**：**当前无任何地方引用**（疑似旧版遗留）；确认后可清理。

### `Views/Today/`（今日页拆出的子卡片）
以下都是"今日页"里的一块小卡片，输入大多是当日相关数据，输出是各自那一块界面；依赖以引擎/数据层/共享件为主（不确定到具体文件）。要改对应那块今日内容时改它：
- `WaterCardView.swift`：喝水卡。
- `HabitsCardView.swift` / `HabitStackView.swift`：习惯清单卡 / 习惯叠放。
- `BentoStatsGrid.swift`：便当格子式统计卡。
- `WeeklyHeatmapView.swift`：一周活跃热力图。
- `GrowthTreeView.swift`：成长树（进度可视化）。**按名称推断**。
- `SkillSpinnerView.swift`：技能转盘。**按名称推断**。
- `MysteryBoxCard.swift`：神秘盒卡片。
- `DailyDiceCard.swift`：每日骰子卡片。
- `MantraCard.swift`：每日格言卡。
- `DailyContentCard.swift`：每日内容卡。
- `CompanionView.swift`：陪伴角色卡。
- `NarrativeBanner.swift`：故事横幅。
- `CelebrationOverlayStack.swift`：今日页里的庆祝浮层叠放区。

## 小组件扩展（`QuickTrackWidget/`）

### `QuickTrackWidget/QuickTrackWidgetBundle.swift`
- **目的**：小组件总入口，登记所有小组件。
- **吃进/吐出**：吃进——无；吐出——可在桌面添加的小组件清单。
- **依赖**：下面各 Widget。
- **何时需要改**：要加/去小组件时。

### `QuickTrackWidget/Widgets/`（各个小组件本体）
每个文件定义一个桌面小组件：定时取数 → 显示。依赖共享件 + 数据适配器（不确定到具体文件）。要改某个小组件的取数/刷新时改它：
- `MedicationWidget.swift`：服药/习惯打卡小组件。
- `QuestStarWidget.swift`：经验/连签进度小组件。
- `SleepWidget.swift`：睡眠小组件。
- `WaterWidget.swift`：喝水小组件。
- `DailyProgressWidget.swift`：每日进度小组件。
- `QuestSessionLiveActivity.swift`：锁屏/灵动岛"任务进行中"实时活动。

### `QuickTrackWidget/WidgetViews/`（小组件的样子）
这些是上面小组件在不同尺寸下的界面画法；输入是要显示的数据，输出是那一块画面；依赖共享件。要改小组件长相时改它：
- `MedicationWidgetView.swift` / `QuestStarWidgetView.swift` / `SleepWidgetView.swift` / `WaterWidgetView.swift` / `DailyProgressWidgetView.swift`：各组件的视图。
- `WidgetComponents.swift`：小组件通用小零件。
- `WidgetLoadState.swift`：加载中/无数据时的占位状态。

## 共享件（`Shared/`，App 和小组件都用）

### `Shared/AppGroupManager.swift`
- **目的**：App 与小组件之间共享设置/密钥的存取（App 写、组件读）。
- **吃进/吐出**：吃进——要共享的设置；吐出——双方都能读到的值。
- **依赖**：被很多处使用（很核心）。
- **何时需要改**：要改共享的配置项时。

### `Shared/Config.swift`
- **目的**：全 App 的配置与常量（含云端地址/键名、经验等设定）——被引用最多。
- **吃进/吐出**：吃进——无；吐出——各种配置常量。
- **依赖**：无（最底层）。
- **何时需要改**：要改任何全局配置时（牵动极广，慎改）。

### `Shared/ColorHex.swift`
- **目的**：把颜色代码转成可用颜色的小帮手。
- **吃进/吐出**：吃进——颜色代码；吐出——颜色。
- **依赖**：无。
- **何时需要改**：要改颜色处理方式时。

### `Shared/QuestSessionAttributes.swift`
- **目的**：定义"任务进行中实时活动"要显示哪些信息。
- **吃进/吐出**：吃进——无；吐出——实时活动的数据形状。
- **依赖**：被实时活动相关文件使用。
- **何时需要改**：要改锁屏实时活动显示内容时。

---

# 第三部分：数据库（`supabase/`）

### `supabase/schema.sql`
- **目的**：云端数据库的"建表蓝图"——定义有哪些数据表、谁能访问哪行数据。
- **吃进/吐出**：吃进——在数据库里执行一次；吐出——建好全部表与权限。
- **依赖**：无。
- **何时需要改**：要加表、加字段或调整访问权限时。

### `supabase/seed-personal.sql`
- **目的**：把项目主人个人的健康习惯安排写入数据库（不放在代码里）。
- **吃进/吐出**：吃进——在数据库里执行；吐出——填入个人专属习惯数据。
- **依赖**：需配合正确的用户编号。
- **何时需要改**：要改个人专属习惯安排时。

---

# 第四部分：配置与脚本（项目根目录等）

### `vite.config.js`
- **目的**：网页应用的打包/开发服务器配置（含开发期的智能接口代理）。
- **吃进/吐出**：吃进——无；吐出——构建与开发的规则。
- **依赖**：无。
- **何时需要改**：要改打包、部署路径或开发代理时。

### `tailwind.config.js` 与 `postcss.config.js`
- **目的**：样式工具（Tailwind）的配置。
- **吃进/吐出**：吃进——无；吐出——可用的样式规则集。
- **依赖**：无。
- **何时需要改**：要改全局样式配置时。

### `package.json`
- **目的**：项目"说明书"——名字、可运行的命令、用到的外部库清单。
- **吃进/吐出**：吃进——无；吐出——给开发工具看的项目信息。
- **依赖**：无。
- **何时需要改**：要加命令或外部库时。

### `vitest.config.js`
- **目的**：自动化测试的运行配置（阶段二新增）。
- **吃进/吐出**：吃进——无；吐出——测试运行规则。
- **依赖**：无。
- **何时需要改**：要改测试运行方式时。

### `tests/characterization/*.test.js`
- **目的**：把某些工具/逻辑的"当前行为"用测试固定下来，防止以后改坏（阶段二新增）。
- **吃进/吐出**：吃进——被测文件；吐出——通过/失败的检查结果。
- **依赖**：分别对应 `gameLogic`、`batchParser`、`icsService`、`useLocalStorage`。
- **何时需要改**：被测文件的预期行为发生**经确认的**变化时。

### `import-12week.js` 与 `import-life-habits.js`
- **目的**：一次性导入脚本——在浏览器控制台粘贴运行，批量灌入预设的学习/生活任务。
- **吃进/吐出**：吃进——粘贴运行；吐出——一批任务被加入。
- **依赖**：无（独立运行，不被应用引用）。
- **何时需要改**：要改这套预设任务内容时。

### `leetcode-import.json`
- **目的**：一份现成的刷题任务数据（供导入用）。
- **吃进/吐出**：吃进——被导入；吐出——一批刷题任务。
- **依赖**：无。
- **何时需要改**：要改这份刷题清单时。

---

## 没有写进来的东西（说明，避免误会）
- 各种说明文档（如 `CLAUDE.md`、`README.md`、本系列报告等）不是"源代码"，未逐一列出。
- 自动生成、不该手改的文件（打包产物 `dist/`、依赖库 `node_modules/`、Xcode 工程缓存等）不在范围内。
- 凡标注"按名称推断"或"不确定"的条目，表示我未逐字确认其内部行为，只据文件名/上下文给出最稳妥的说法——动这些文件前建议先打开看一眼。

*本文件只描述用途与关系，不描述任何内部实现细节。*
