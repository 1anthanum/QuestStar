# BEHAVIOR_SPEC.md — 行为规格（验收标准）

本文件用普通话、从「用户能看到什么」的角度，描述本产品所有可观察的行为。
**它是后续一切重构的验收标准。** 任何改动都不得违反其中任意一条。
若某次改动确实需要修改某条规则，**必须先在对话里和我确认，确认后再改本文件**。

阅读约定：
- 正常行为用「**当** … ，系统应该 … 」。
- 异常行为用「**如果** … ，系统应该 … 」。
- 文末「**安静路径**」列出**不应该产生任何副作用**的操作。
- 本文件只讲「看得见的结果」，不讲「内部怎么做到的」。
- 文末「**待确认清单**」列出当前实现中存疑、尚未纳入验收的行为——这些需要你拍板。

---

## 1. 进入应用与账号

- 当用户首次打开应用且该部署设置了访问密码时，系统应该先显示一个密码输入界面，未通过前不显示主界面。
- **如果**用户输入的访问密码不正确，系统应该提示密码错误并停留在密码界面，不放行。
- 当用户通过密码界面（或该部署未设密码）后，系统应该直接进入可用的主界面，即使没有登录账号。
- 当用户未登录时，系统应该以「访客」身份正常使用全部本机功能。
- 当用户选择登录或注册时，系统应该提供「邮箱+密码」「用 Google 登录」「用 GitHub 登录」三种方式。
- 当用户成功登录后，系统应该显示其账号标识（如名字首字母的彩色圆点），并把本机已有的数据与该账号关联起来。
- **如果**登录或注册失败（密码错误、邮箱已占用、网络不通等），系统应该显示明确的失败原因，并保持在未登录状态，不清空用户已填的内容。
- 当已登录用户选择退出登录时，系统应该回到访客状态，且本机数据仍然可见可用。

## 2. 任务与步骤（核心）

- 当用户手动创建一个任务时，系统应该要求至少有任务名称，创建后该任务出现在任务列表中。
- 当用户打开一个任务时，系统应该显示它的全部步骤、完成进度，以及（若有）截止日期。
- 当用户编辑任务或步骤（改名称、改难度、改截止日期、增删步骤）并保存后，系统应该立即在界面上反映这些改动。
- 当用户拖动调整步骤顺序（在普通列表视图中）时，系统应该按新顺序显示并保留这个顺序。
- 当用户请求删除一个任务时，系统应该先弹出确认提示。
  - **如果**用户在确认提示中取消，系统应该不删除任何内容。
  - 当用户在确认提示中确认后，系统应该删除该任务并从列表中移除。
- 当用户把一个步骤标记为完成时，系统应该把它显示为已完成，并触发第 3 节的完成反馈。
- 当用户取消一个步骤的完成标记时，系统应该把它显示为未完成，并且**不发放任何奖励**，也**不扣回**之前已获得的经验值。
- 当一个任务的所有步骤都完成时，系统应该把该任务显示为已完成。

## 3. 完成步骤后的即时反馈

- 当用户完成一个步骤时，系统应该展示一次获得经验值的提示动画。
- 当用户完成一个步骤时，系统应该有一定概率（约 8%）额外给予一笔 1～5 元的随机金币惊喜；没有触发时不应给金币。
- 当用户完成一个步骤时，系统应该有一定概率掉落一枚知识碎片（普通任务约 12%，挑战类任务约 18%）；掉落时给出可见的碎片提示。
- 当用户完成一个步骤后片刻，系统应该推荐一个合理的「下一步」卡片；该卡片在约 8 秒后自动消失，用户也可手动关闭或点击前往。
- 当用户当天累计完成满 5 个步骤时，系统应该额外给予 2 元奖励（每天一次）。
- 当用户把当天的全部日常任务都完成时，系统应该额外给予 10 元「全清」奖励（每天一次）。
- 当完成步骤导致经验值跨过升级线时，系统应该展示一次升级庆祝。
- 当完成步骤导致整个任务首次全部完成时，系统应该展示一次任务完成庆祝。
- 上述各项反馈**只在「未完成 → 完成」这一次切换时发生**；取消完成不触发其中任何一项。

## 4. 经验值与等级

- 当用户完成一个步骤时，系统应该按步骤难度给予经验值：简单 +10、中等 +20、困难 +35、未标难度 +15。
- 当用户完成一个步骤时，系统应该按当前连续天数给予加成：每多一天 +10%，最高 +50%。
- 当用户完成一个步骤时，系统应该按任务类型给予倍率：日常 ×1、额外 ×1.5、挑战 ×2。
- 当用户当天**第一次**完成任意步骤时，系统应该额外给予 25 点经验值（每天一次）。
- 当用户首次完成整个任务时，系统应该额外给予 50 点经验值。
- 当用户的累计经验值达到下一等级门槛时，系统应该提升其等级；共 10 个等级，从 0 到 5000。
- 当用户处于最高等级时，系统应该把进度显示为已满，且不再要求更多经验值升级。

## 5. 连续天数与断签保护

- 当用户在连续的相邻两天都完成过步骤时，系统应该把连续天数 +1。
- 当用户在同一天内多次完成步骤时，系统应该让连续天数保持不变（同一天不重复累加）。
- 当用户漏掉若干天后再次完成步骤时，系统应该减少连续天数（按断签处理），但**不清零**。
- 当用户达到连续天数里程碑时，系统应该给予对应奖励：第 3 天 5 元、第 5 天 15 元、第 10 天 12 元、第 14 天 25 元、第 21 天 30 元、第 30 天 50 元（每个里程碑只发一次）。
- 系统应该提供「每周一次」的断签保护，用于在一次漏签时避免连续天数受损（其确切触发方式见文末待确认清单）。

## 6. 奖励钱包

- 当用户获得任何金币奖励时，系统应该把它累加到钱包余额，并在奖励记录中留下一条可见的流水。
- 当用户查看奖励面板时，系统应该显示钱包余额、各里程碑的「已领取 / 可领取 / 未解锁」状态。
- 当用户花费钱包余额（如在承诺契约中下注）时，系统应该相应减少余额并记录流水。
- **如果**用户尝试花费超过余额的金额，系统应该拒绝该操作，余额不变。

## 7. 智能拆解 / 批量导入 / 文件导入

- 当用户输入一个目标并请求「智能拆解」时，系统应该生成一组有层次的步骤供预览，用户确认后才创建任务。
- 当用户在拆解前选择不同的深度（快速 / 标准 / 深入）时，系统应该据此生成数量不同的步骤。
- 当用户在生成结果上要求「更详细 / 更简洁 / 自定义反馈 / 重新生成」时，系统应该据此调整步骤列表。
- 当用户粘贴或上传一份大纲并批量导入时，系统应该解析出可勾选的条目，允许加分组标签后再批量创建任务。
- 当用户导入文件（用于摘要或提取步骤）时，系统应该给出可用于创建任务的结果。
- **如果**用户未设置任何智能服务密钥就请求需要联网智能的功能，系统应该提示去设置中填写密钥，并且不创建空任务。
- **如果**智能服务调用失败（网络错误、密钥无效、额度用尽等），系统应该显示失败提示，保留用户已输入的内容，且不创建残缺任务。
- **如果**导入的文本为空或无法解析出任何条目，系统应该提示无可导入内容，且不创建任务。

## 8. 学习模式 / 生活模式

- 当用户在「学习」与「生活」两个模式间切换时，系统应该只改变所显示的板块与任务范围，不改变任何已有数据。
- 当处于「学习」模式时，系统应该展示与学习相关的板块（如知识树、挑战、学习路线、微学习等）。
- 当处于「生活」模式时，系统应该展示与生活习惯相关的板块（如习惯仪表盘、每日时间块打卡等）。

## 9. 生活习惯每日打卡

- 当用户在某个时间块里勾选一项习惯活动时，系统应该把它标为今日已完成，并更新当日完成度。
- 当进入新的一天时，系统应该把习惯打卡重置为未完成（昨天的勾选不带入今天）。
- 当用户进入编辑模式增删活动、修改时间段时，系统应该保存这些自定义内容并在之后沿用。
- 当用户从未自定义过时间块时，系统应该显示一套通用默认安排。

## 10. 知识碎片与书籍

- 当用户收集到碎片时，系统应该把它计入收藏，并按所属「书」归类展示。
- 当用户集齐某本书的全部碎片时，系统应该解锁该书的总结/路径卡。
- 当用户查看背包/收藏时，系统应该汇总展示已掌握技能、已收集碎片、奖励里程碑三类内容。

## 11. 概念掌握（开花）

- 当用户「接触」一个新概念时，系统应该记录其进入掌握流程，并随时间推进其阶段。
- 当用户对某概念做出归宿选择（深入 / 休眠 / 连接 / 放手）时，系统应该据此改变其后续走向与展示。
- 当用户当天接触的新概念已达上限（5 个）或当天总操作已达上限（10 次）时，系统应该不再接受新的此类操作。

## 12. 反思与心情

- 当用户进入反思功能时，系统应该提供多种反思方式（如一键快记、篝火问答、陪伴对话、随机提问、身体感受、心情地形）。
- 当用户记录某天的反思或心情后，系统应该保存该记录，并在之后可被回看。
- 当用户重复编辑同一天的反思时，系统应该更新该天的记录，而不是新建重复条目。

## 13. 防拖延与专注工具

- 当用户打开「智能启动器（就做这一件）」时，系统应该只推荐一个当下最该做的步骤。
- 当某个任务长期停滞时，系统应该以可见方式提示（如变灰），并允许把它拆成更小的微步骤或一键「救援」恢复。
- 当用户使用能量调度时，系统应该根据其标记的当前精力给出难度匹配的建议。
- 当用户开启「幽灵竞速」时，系统应该把今天的完成进度与上周同一天作对比并显示领先/落后状态。
- 当存在逾期任务时，系统应该允许把它们汇总成一场「Boss 战」，并在清掉后给予额外庆祝奖励。
- 当用户设立「承诺契约」（押注一笔钱在期限内完成若干步骤）时：
  - 当用户在期限内达成目标时，系统应该退还押金并给予额外奖励。
  - **如果**到期仍未达成，系统应该没收押金。
- 当用户开启「双轨切换」时，系统应该允许在两个不同类别的任务间来回切换并记录进度。
- 当用户进入「专注模式」时，系统应该提供无干扰的计时执行界面，并支持以键盘快捷方式完成步骤、暂停、退出。

## 14. 日历与日程导入导出

- 当用户查看日历时，系统应该按截止日期把任务标注在对应日期上。
- 当用户导出日程时，系统应该生成一份标准日历文件，包含带截止日期的任务与每日习惯。
- 当用户导入一份标准日历文件时，系统应该解析其中的事件并按日期排序展示，无日期的排在最后。
- **如果**导入的日历文件无法识别或为空，系统应该不导入任何事件，并给出提示。

## 15. 截止提醒

- 当某任务临近或到达截止时间且用户已授权通知时，系统应该发出一次提醒，且同一情形不应在同一天重复打扰。
- **如果**用户拒绝了通知权限，系统应该不弹出系统通知，应用其余功能照常运行。

## 16. 外观与语言

- 当用户切换主题时，系统应该立即改变整体配色（共 6 套主题），其余内容不变。
- 当用户切换语言（中文 / 英文）时，系统应该立即把界面文字切换为对应语言，其余数据不变。
- 当用户下次再打开应用时，系统应该沿用其上次选择的主题与语言。

## 17. 设置与数据导入导出

- 当用户在设置中填写某智能服务的密钥与模型后，系统应该在之后的智能功能中使用它。
- 当用户测试某服务连接时，系统应该显示连接成功或失败的结果。
- 当用户导出本机数据时，系统应该生成一份可重新导入的备份。
- 当用户导入一份备份时，系统应该恢复其中的数据。
- **如果**导入的备份文件损坏或格式不符，系统应该拒绝导入并提示，不破坏现有数据。

## 18. 数据保存与跨设备

- 当用户做出任何会改变数据的操作后，系统应该自动保存；刷新或重新打开应用后这些数据仍然存在。
- 当已登录用户在一台设备上修改数据后，这些改动稍后应能在该用户的其他设备上看到。
- **如果**保存到云端暂时失败（离线、服务不可用等），系统应该让本机继续可用、改动不丢失，并在恢复后继续同步。
- **如果**本机已保存的数据出现个别损坏/无法读取，系统应该回退到该项的默认值而不是整体崩溃。
- **如果**界面某个区域发生意外错误，系统应该以错误兜底界面替代该区域，而不是让整页白屏。

---

## 安静路径（以下操作不应该产生任何副作用）

「副作用」指：改变任务/步骤、增减经验值、改变连续天数、增减钱包或碎片、发放奖励、触发庆祝、上传数据、发出通知。

- 仅浏览任务列表、打开任务详情查看、滚动各面板——不应改变任何数据，也不应发放奖励。
- 打开任意弹窗/面板后**未做提交**就关闭（创建任务弹窗、设置、奖励面板、背包、日历、智能拆解预览等）——不应改变任何数据。
- 切换主题、切换语言——只改变外观与文字，不应改动任务、经验值、连续天数、钱包、碎片。
- 切换「学习/生活」模式、切换任务的分组标签筛选——只改变所显示的内容，不应改动任何数据。
- 取消一个步骤的完成标记——不应发放奖励、不应改连续天数、不应扣回经验值。
- 在访客（未登录）状态下的任何操作——不应把数据上传到任何账号。
- 测试智能服务连接——只反馈连通性，不应创建任务或扣除任何额度类资源。
- 当智能拆解/批量导入只是「预览」尚未确认时——不应创建任何任务。
- 当某天接触新概念已达上限后再次尝试——不应改变掌握进度（安静地不生效）。

---

## 待确认清单（当前实现存疑，尚未纳入验收，请你拍板）

以下行为在当前代码中是这样的，但是否要作为「必须保持」的规则，需要你确认。
（在确认前，重构既不要刻意保留、也不要刻意改变它们；要动到这些，请先和我说。）

1. **重复勾选可再次得奖**：把一个已完成步骤「取消完成」后再次「完成」，当前会**再次**发放经验值/金币/碎片等。是按现状锁定，还是应视为刷奖励漏洞予以禁止？
2. **断签保护的触发**：第 5 节的「每周一次护盾」在当前完成步骤的流程中似乎未实际生效，确切触发条件不明确。需确认护盾应在何种情形、以何种可见方式生效。
3. **未来日期被当作断签**：如果设备时间被调到「未来」或时区异常，当前会把连续天数按断签扣减。是否需要规定「时间异常时连续天数不受损」？
4. **缺失数值的展示**：极端情况下（如经验值数据缺失）当前可能在界面显示为「NaN」。是否需要规定「任何数值异常都应显示为 0 或占位符，绝不显示 NaN」？
5. **空步骤任务的日历状态**：导出日历时，没有任何步骤的任务当前会被标为「已完成」。是否符合预期？

> 说明：第 1–5 条对应阶段一 [ISSUES.md](ISSUES.md) 中已记录的可疑行为；列在此处是为了让验收标准与「已知问题」边界清晰，避免把疑似缺陷悄悄固化成「必须保持的规则」。

---

*本文件描述用户可见行为，不描述任何内部实现。最后更新：随阶段二同时建立。*
